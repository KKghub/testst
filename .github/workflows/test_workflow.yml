name: Test Workflow

on:
  pull_request:

env:
  GLOBAL_VAR: 7

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: Set Output
        id: step-1
        run: echo "Hi"

  job-2:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test-case: [case-1, case-2]
        include:
          - test-case: case-1
            x: 1
            y: 2
          - test-case: case-2
            x: 3
            y: 4
    env:
      BOX_TYPE: cozy
    needs: job-1
    steps:
      # - name: Checkout
      #   uses: actions/checkout@v2
      - name: command test
        run: |
          echo $BOX_TYPE
          echo ${{ env.BOX_TYPE }}
          echo ${{ matrix.x }}
          echo ${{ matrix.y }}
          echo ${{ env.GLOBAL_VAR }}
          pwd
          ls -al
